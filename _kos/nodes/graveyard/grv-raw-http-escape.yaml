id: grv-raw-http-escape
type: graveyard
confidence: graveyard
title: "raw HTTP escape hatch (inherited ruling)"
content: |
  Same ruling as sidestep G1: the spec is the canonical surface; `raw`
  would let drift hide. Update the spec and regenerate instead.
  `stave api <operationId>` is the spec-aware escape hatch.
edges:
  - target: elem-codegen-iru-prepasses
    type: contradicts
    note: "raw bypasses the spec; codegen treats the spec as the contract"
graveyard:
  approach: "stave raw <method> <path> — send arbitrary HTTP requests"
  context: "Some iru endpoints might not be in the spec yet."
  finding: "An escape hatch lets drift hide. The spec is the contract."
  ruling: "Use stave api <operationId>. If a real op is missing, sync-spec and regenerate."
  reopener: "Concrete need for an undocumented endpoint iru declines to add to the spec."
provenance:
  created_by: human
  created_at: 2026-07-15
  session: scaffold
