id: elem-live-validated
type: element
confidence: bedrock
title: "Live-validated against the production tenant (B8)"
content: |
  Same-day follow-up after the user granted token read permissions.
  Auth verified by differential probe (garbage → 401, none → 401,
  real → 403 pre-grant): the token IS the bearer, no exchange step.
  Smoke: licensing payload real; list live for 10/12 kinds with EVERY
  kind-table field guess confirmed; live CEL pipeline on real fleet
  data; MCP end-to-end (call success, guard refusal, map 35/131);
  audit trail 37 JSONL lines day one at ~/.stave/audit/ (macOS —
  dirs::state_dir() is None there; XDG state path applies on Linux).

  threat + behavioral_detection 403 — the only unverified pair (see
  question-response-shape-gap).
edges: []
provenance:
  created_by: human
  created_at: 2026-07-15
  session: scaffold
