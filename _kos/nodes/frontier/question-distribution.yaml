id: question-distribution
type: question
confidence: frontier
title: "Distribution residue — signed alpha verified, stable tag pending"
content: |
  Repo public (2026-07-15, post credential/tenant scan). CI green on
  curtain's upstream action pins; scorecards/dependency-review guards
  self-activated on the public flip. Signing enabled: release
  environment (main + v* + alpha-*), 7 org secrets granted,
  SIGNING_ENABLED=true. Remaining: verify the signed alpha + tap
  formula end-to-end (`brew install ArcavenAE/tap/stave`), mise
  flow, then first v* stable when the surface settles.
  bd: aae-orc-b6dg.8.
edges:
  - target: elem-sidestep-pattern-port
    type: derives
    note: "the alpha/release/tap pipeline is part of the ported pattern"
provenance:
  created_by: human
  created_at: 2026-07-15
  session: scaffold
