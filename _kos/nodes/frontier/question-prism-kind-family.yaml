id: question-prism-kind-family
type: question
confidence: frontier
title: "Prism as a parameterized kind family?"
content: |
  The 16 get_prism_* report endpoints (apps, filevault, certificates,
  kernel extensions, ...) share a uniform query shape (blueprint_ids,
  device_families, filter, sort_by, limit/offset). They'd fit a
  parameterized kind (`stave list prism --category apps`) better
  than 16 separate kinds. Defer to v0.2 with audit-mining evidence;
  today they are reachable via `stave api get_prism_<category>`.
edges:
  - target: question-audit-mining
    type: derives
    note: "the mining data decides whether prism earns kind status"
provenance:
  created_by: human
  created_at: 2026-07-15
  session: scaffold
