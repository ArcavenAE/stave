# Provisional until live introspection (see charter F1).
query ListCloudResources($first: Int, $after: String) {
  cloudResources(first: $first, after: $after) {
    nodes {
      id
      name
      type
      cloudPlatform
      subscriptionExternalId
    }
    pageInfo {
      hasNextPage
      endCursor
    }
  }
}
