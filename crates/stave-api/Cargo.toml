[package]
name = "stave-api"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true
description = "Curated GraphQL operation library for the Wiz API. Documents validated against the vendored schema via `cargo xtask check-ops`."

[lib]
path = "src/lib.rs"

[dependencies]
